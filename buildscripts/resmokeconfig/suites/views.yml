selector:
  js_test:
    roots:
    - jstests/views/*.js

executor:
  js_test:
    config:
      shell_options:
        readMode: commands
    hooks:
    fixture:
      class: MongoDFixture
      mongod_options:
        set_parameters:
          enableTestCommands: 1
